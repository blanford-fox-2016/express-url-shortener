extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="/", method="post", name="form_url")
    input(type="text", name="input_url", placeholder="Paste a link to shorten it")
    input(type="submit", value="Shorten")
